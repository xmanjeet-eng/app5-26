services:
  - type: web
    name: nifty-predictor
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
      - key: DEBUG
        value: false
    healthCheckPath: /api/health
    autoDeploy: true
